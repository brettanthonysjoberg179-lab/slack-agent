# Slack Agent

Minimal Slack messaging agent.

## Install

```bash
python -m venv .venv
.venv/bin/pip install .
```

## Usage

Set `SLACK_BOT_TOKEN`, then call `post_message` or `reply_to_thread`.

## Tests

```bash
pytest
```
