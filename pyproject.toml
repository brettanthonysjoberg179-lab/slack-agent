[project]
name = "slack-agent"
version = "0.1.0"
description = "Minimal Slack messaging agent"
requires-python = ">=3.11"
dependencies = ["httpx>=0.27"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
